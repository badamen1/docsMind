/**
 * @deprecated — Usa `useAuth` del AuthContext directamente.
 * Este archivo se mantiene por compatibilidad.
 * Import correcto: import { useAuth } from "@/lib/contexts/AuthContext";
 */
export { useAuth } from "@/lib/contexts/AuthContext";
